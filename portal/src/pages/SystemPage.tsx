import { useState } from 'react';
import type { DeviceStatus, LogEntry } from '../../../shared/schemas/models';
import { Button, SectionHeading, StatCard, StatusBadge, formatBytes, formatUptime } from '../components/Ui';

interface Props { status: DeviceStatus; logs: LogEntry[]; onReboot: () => Promise<void>; onNotify: (message: string, kind?: 'success' | 'error' | 'info') => void; }

export function SystemPage({ status, logs, onReboot, onNotify }: Props) {
  const [updating, setUpdating] = useState(false);
  const startOta = () => { setUpdating(true); onNotify('Firmware upload is gesimuleerd in mock mode', 'info'); window.setTimeout(() => setUpdating(false), 1800); };
  return <div className="page-stack"><SectionHeading eyebrow="DEVICE ADMINISTRATION" title="Systeem" description="Firmware, verbinding, logging en onderhoud van de controller." action={<StatusBadge tone={status.online ? 'success' : 'danger'}>{status.online ? 'Healthy' : 'Attention'}</StatusBadge>} /><div className="stats-grid stats-grid--system"><StatCard label="Firmware" value={status.firmware} detail="ESP32-S3 build" icon="◇" tone="purple" /><StatCard label="Vrij heap" value={formatBytes(status.heapFree)} detail="runtime memory" icon="⌁" tone="blue" /><StatCard label="Vrij PSRAM" value={formatBytes(status.psramFree)} detail="DMA frame buffer" icon="▥" tone="green" /><StatCard label="Uptime" value={formatUptime(status.uptime)} detail="since last boot" icon="◷" tone="orange" /></div><div className="system-grid"><section className="panel"><div className="panel-heading"><div><span className="eyebrow">FIRMWARE</span><h2>OTA update</h2></div><StatusBadge tone="neutral">Ready</StatusBadge></div><div className="firmware-card"><div className="firmware-icon">⇧</div><div><strong>{status.firmware}</strong><p>Upload een firmware.bin bestand naar de ESP32 zonder configuratie te wissen.</p></div></div><label className="upload-field"><input type="file" accept=".bin" onChange={startOta} /><span>Bestand kiezen</span><small>firmware.bin · max 4 MB</small></label>{updating && <div className="progress"><div className="progress__bar" /><span>Uploaden en valideren…</span></div>}<div className="form-actions"><Button variant="primary" disabled={updating}>Firmware uploaden</Button><Button onClick={() => void onReboot()}>Restart device</Button></div></section><section className="panel"><div className="panel-heading"><div><span className="eyebrow">CONNECTIVITY</span><h2>Verbinding</h2></div></div><div className="details-list"><div><span>WiFi status</span><strong className={status.online ? 'text-success' : 'text-danger'}>{status.online ? 'Verbonden' : 'Offline'}</strong></div><div><span>Hostname</span><strong>{status.hostname}</strong></div><div><span>IP adres</span><strong>{status.ip}</strong></div><div><span>RSSI</span><strong>{status.wifiRssi} dBm</strong></div><div><span>NTP</span><strong>{status.timeSynced ? 'Gesynchroniseerd' : 'Niet beschikbaar'}</strong></div><div><span>Timezone</span><strong>Europe/Amsterdam</strong></div></div></section></div><section className="panel"><div className="panel-heading"><div><span className="eyebrow">RINGBUFFER · 100 ITEMS</span><h2>Logs</h2></div><Button onClick={() => onNotify('Logs ververst')}>Verversen</Button></div><div className="log-list">{logs.slice(0, 12).map((log) => <div className="log-row" key={log.id}><time>{new Date(log.timestamp).toLocaleTimeString('nl-NL', { hour: '2-digit', minute: '2-digit', second: '2-digit' })}</time><span className={`log-level log-level--${log.level.toLowerCase()}`}>{log.level}</span><code>{log.category}</code><p>{log.message}</p></div>)}</div></section><section className="panel danger-zone"><div><span className="eyebrow">MAINTENANCE</span><h2>Backup & reset</h2><p>Exporteer de huidige JSON-configuratie of maak de controller klaar voor een nieuwe installatie.</p></div><div className="danger-actions"><Button onClick={() => onNotify('Configuratie-export klaar', 'info')}>Config exporteren</Button><Button onClick={() => onNotify('Importeer een JSON-bestand via de ESP32 API', 'info')}>Config importeren</Button><Button variant="danger" onClick={() => onNotify('Factory reset vereist nog een fysieke bevestiging', 'info')}>Factory reset</Button></div></section></div>;
}

